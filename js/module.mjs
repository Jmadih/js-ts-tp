// By default every element declared in a module is local, to make it visble outside the module you should use export 

export class myModule{
    constructor(name){
        this._name = name;
    }
}
